# IDEAS
- when task is inserted into day sort it into the block comparing with each tasks objective contribution
    - for example inserting p4 (highest) iterate over the "coresponding stack" insert it before tasks with lower contribution (like p3 task)